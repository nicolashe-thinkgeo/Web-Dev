#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mini/bin/_mini" "$@"
  ret=$?
else 
  node  "$basedir/../mini/bin/_mini" "$@"
  ret=$?
fi
exit $ret
